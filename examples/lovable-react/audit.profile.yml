# audit.profile.yml — EXAMPLE
# This file is generated by vibe-audit on first run.
# Replace all values with your app's actual values.
# NEVER commit this file — it contains credentials.

app:
  name: "My Lovable App"
  frontend_url: "https://my-app.lovable.app"
  backend_url: "https://my-api.up.railway.app"

auth:
  email: "admin@myapp.com"
  password: "mypassword"
  success_url_contains: "/dashboard"

delivery_contacts:
  phone: "+1XXXXXXXXXX"
  email: "admin@myapp.com"

local_paths:
  frontend: "src"
  backend: "api"
  playwright_auth_state: "tests/.auth/state.json"

trap_library: "tests/audit/trap-library.md"
